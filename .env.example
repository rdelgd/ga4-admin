# GA4 Admin Tool Environment Variables
# Copy this file to .env and fill in your actual values

# =============================================================================
# GOOGLE AUTHENTICATION
# =============================================================================

# Path to your Google Cloud service account key file (JSON format)
# This file contains the credentials needed to authenticate with Google Analytics Admin API
# Download from: Google Cloud Console > IAM & Admin > Service Accounts
# Make sure the service account has "Google Analytics Admin" permissions
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Alternative: If using Application Default Credentials (ADC), you can leave this empty
# ADC works if you're running on Google Cloud or have gcloud CLI configured locally

# =============================================================================
# GA4 CONFIGURATION
# =============================================================================

# Your GA4 Property ID in the format "properties/XXXXXXXXX"
# Find this in: Google Analytics > Admin > Property Settings
# Example: properties/123456789
GA4_PROPERTY_ID=properties/YOUR_PROPERTY_ID_HERE

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================

# 1. Set up Google Cloud Service Account:
#    - Go to Google Cloud Console
#    - Create or select a project
#    - Enable the Google Analytics Admin API
#    - Create a service account with "Google Analytics Admin" role
#    - Download the JSON key file
#    - Set GOOGLE_APPLICATION_CREDENTIALS to the path of this file

# 2. Find your GA4 Property ID:
#    - Go to Google Analytics
#    - Navigate to Admin > Property Settings
#    - Copy the Property ID (it will be a number like 123456789)
#    - Format it as "properties/123456789"

# 3. Grant permissions:
#    - In Google Analytics Admin, go to Property Access Management
#    - Add your service account email as a user with Editor permissions

# 4. Run the tool:
#    node index.mjs
#    # Or override property via CLI: node index.mjs --property=properties/123456789

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Default channel group name (can be overridden with --group CLI flag)
# DEFAULT_CHANNEL_GROUP_NAME="Custom Channel Group"

# =============================================================================
# SECURITY NOTES
# =============================================================================

# IMPORTANT: Never commit your actual .env file to version control!
# The .env file should be added to your .gitignore
# Only commit this .env.example file as a template

# Make sure your service account key file is also excluded from version control
# Add it to .gitignore: *.json (or be more specific with the filename)
